# Setup virtual environment and install dependencies
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Run the test client
python test_client.py --base_url='http://localhost:28100'
